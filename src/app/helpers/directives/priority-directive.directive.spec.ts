import { PriorityDirectiveDirective } from './priority-directive.directive';

describe('PriorityDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PriorityDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
